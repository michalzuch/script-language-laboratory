# Amazon Crawler in Ruby

## Prerequisites

Install [Docker](https://www.docker.com/)

## Running the Application

```bash
docker-compose up --build
```

## Configuration

You can modify the search keyword by changing the command argument in the `Dockerfile`.
